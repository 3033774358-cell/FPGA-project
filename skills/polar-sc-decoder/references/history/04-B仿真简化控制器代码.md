# B+仿真简化控制器代码

*导出时间: 2026/8/12 16:42:58*

---

### User

我们现在要给这个程序写SC译码，我现在先给你顶层，后续会给你其他内部模块
module polar_encode_chain #(
    parameter integer NMAX    = 1024,
    parameter integer MSG_MAX = 2048       // 支持的最大输入信息比特数
)(
    input  wire                 clk,
    input  wire                 rst_n,
    input  wire                 start,
    input  wire [MSG_MAX-1:0]   msg,       // 输入信息比特, msg[0]=首比特
    input  wire [15:0]          B,         // 信息比特总长
    input  wire [3:0]           mcs,       // 表22 调制编码方式索引 0..12
    input  wire                 ftype2,    // 1=无线帧类型2(6.9.1.2); 0=类型3/4(6.9.1.3)
    input  wire [23:0]          crc_seed,  // 码块CRC24B生成种子 (广播类: 0x555555)
    output reg                  busy,
    output reg                  done,      // 整链输出完毕 (单拍)
    output wire                 m_valid,   // 级联后串行输出
    output wire                 m_bit,
    output wire [31:0]          k_count,
    output reg  [7:0]           cb_count   // 本次产生的码块总数 (观测/校验)
);
    // ---------------- 协议表格 ----------------
    // 表22: MCS -> r16 (= 编码速率R × 16; 调制方式与信道编码无关)
    function [4:0] tab22_r16;
        input [3:0] m;
        case (m)
            4'd0, 4'd2:  tab22_r16 = 5'd4;    // R=1/4  (BPSK/QPSK)
            4'd1, 4'd3:  tab22_r16 = 5'd6;    // R=3/8
            4'd4:        tab22_r16 = 5'd8;    // R=1/2
            4'd5, 4'd9:  tab22_r16 = 5'd10;   // R=5/8
            4'd6, 4'd10: tab22_r16 = 5'd12;   // R=3/4
            4'd7, 4'd11: tab22_r16 = 5'd14;   // R=7/8
            4'd8, 4'd12: tab22_r16 = 5'd16;   // R=1
            default:     tab22_r16 = 5'd8;    // 非法MCS按1/2 (spec 仅 0..12)
        endcase
    endfunction

    // 表20: index(0..14) -> {N512[1:0], N256[1:0], N128[1:0]}
    function [5:0] tab20;
        input [3:0] idx;
        case (idx)
            4'd0:  tab20 = {2'd0,2'd0,2'd0};   4'd1:  tab20 = {2'd0,2'd0,2'd1};
            4'd2:  tab20 = {2'd0,2'd0,2'd2};   4'd3:  tab20 = {2'd0,2'd1,2'd1};
            4'd4:  tab20 = {2'd0,2'd1,2'd2};   4'd5:  tab20 = {2'd0,2'd2,2'd1};
            4'd6:  tab20 = {2'd0,2'd2,2'd2};   4'd7:  tab20 = {2'd1,2'd1,2'd1};
            4'd8:  tab20 = {2'd1,2'd1,2'd2};   4'd9:  tab20 = {2'd1,2'd2,2'd1};
            4'd10: tab20 = {2'd1,2'd2,2'd2};   4'd11: tab20 = {2'd2,2'd1,2'd1};
            4'd12: tab20 = {2'd2,2'd1,2'd2};   4'd13: tab20 = {2'd2,2'd2,2'd1};
            default: tab20 = {2'd2,2'd2,2'd2}; // idx=14 (及钳位)
        endcase
    endfunction

    // 表23: 类型3/4 末块细分 速率适配 K 值 (码率 R-1/16); nsel: 0=码长512, 1=256, 2=128, 3=64
    //   数值直接照抄 spec 表23 (第一表格) 原格, 便于逐格对照; 等价于 N×(R-1/16)。
    //   (码长1024 列 = 1024R, 与满块相同, 由 K1024 给出, 不在本函数内。)
    function [9:0] tab23;
        input [4:0] r16v;
        input [1:0] nsel;
        case ({r16v, nsel})
            {5'd4, 2'd0}: tab23 = 10'd96;   {5'd4, 2'd1}: tab23 = 10'd48;   // R=1/4
            {5'd4, 2'd2}: tab23 = 10'd24;   {5'd4, 2'd3}: tab23 = 10'd12;
            {5'd6, 2'd0}: tab23 = 10'd160;  {5'd6, 2'd1}: tab23 = 10'd80;   // R=3/8
            {5'd6, 2'd2}: tab23 = 10'd40;   {5'd6, 2'd3}: tab23 = 10'd20;
            {5'd8, 2'd0}: tab23 = 10'd224;  {5'd8, 2'd1}: tab23 = 10'd112;  // R=1/2
            {5'd8, 2'd2}: tab23 = 10'd56;   {5'd8, 2'd3}: tab23 = 10'd28;
            {5'd10,2'd0}: tab23 = 10'd288;  {5'd10,2'd1}: tab23 = 10'd144;  // R=5/8
            {5'd10,2'd2}: tab23 = 10'd72;   {5'd10,2'd3}: tab23 = 10'd36;
            {5'd12,2'd0}: tab23 = 10'd352;  {5'd12,2'd1}: tab23 = 10'd176;  // R=3/4
            {5'd12,2'd2}: tab23 = 10'd88;   {5'd12,2'd3}: tab23 = 10'd44;
            {5'd14,2'd0}: tab23 = 10'd416;  {5'd14,2'd1}: tab23 = 10'd208;  // R=7/8
            {5'd14,2'd2}: tab23 = 10'd104;  {5'd14,2'd3}: tab23 = 10'd52;
            default:      tab23 = 10'd0;    // R=1 不细分, 不会取用
        endcase
    endfunction

    // 表24: 类型2 速率适配 K 值; nsel: 0=码长512, 1=256, 2=128, 3=64
    function [9:0] tab24;
        input [4:0] r16v;
        input [1:0] nsel;
        case ({r16v, nsel})
            {5'd10,2'd0}: tab24 = 10'd316;  {5'd10,2'd1}: tab24 = 10'd156;
            {5'd10,2'd2}: tab24 = 10'd74;   {5'd10,2'd3}: tab24 = 10'd36;
            {5'd12,2'd0}: tab24 = 10'd382;  {5'd12,2'd1}: tab24 = 10'd189;
            {5'd12,2'd2}: tab24 = 10'd90;   {5'd12,2'd3}: tab24 = 10'd45;
            {5'd14,2'd0}: tab24 = 10'd446;  {5'd14,2'd1}: tab24 = 10'd221;
            {5'd14,2'd2}: tab24 = 10'd106;  {5'd14,2'd3}: tab24 = 10'd53;
            default:      tab24 = 10'd0;    // 类型2 仅支持表24所列速率
        endcase
    endfunction

    // n∈{0,1,2} 与 K 的乘积 (类型2 rem 计算用)
    function [10:0] mul012;
        input [1:0] c;
        input [9:0] K;
        case (c)
            2'd0:    mul012 = 11'd0;
            2'd1:    mul012 = {1'b0, K};
            default: mul012 = {K, 1'b0};
        endcase
    endfunction

    // ---------------- 状态机 ----------------
    localparam [4:0]
        S_IDLE      = 5'd0,
        SB_FULL_INIT= 5'd1,  SB_LAST_INIT= 5'd2,  SB_QINIT    = 5'd3,
        SB_QLOOP    = 5'd4,  SB_SUBNEXT  = 5'd5,
        SA_INIT     = 5'd6,  SA_N1024    = 5'd7,  SA_IDX      = 5'd8,
        SA_REM      = 5'd9,  SA_N64      = 5'd10, SA_BLKNEXT  = 5'd11,
        SA_PAD      = 5'd12,
        S_FZ        = 5'd13, S_ASM       = 5'd14, S_ENC       = 5'd15,
        S_CATW      = 5'd16, S_ROUTE     = 5'd17, S_DONE      = 5'd18;
    reg [4:0] state;
    localparam [1:0] PH_BFULL=2'd0, PH_BSUB=2'd1, PH_A=2'd2;
    reg [1:0] phase;

    // ---------------- 锁存与派生量 ----------------
    reg  [MSG_MAX-1:0] msg_sh;             // 消息移位寄存器 (消费一位移一位, 零变址 mux)
    reg  [15:0] B_l;
    reg  [15:0] mp;                        // 已消费消息位数 (仅计数, 供类型2 avail)
    reg  [4:0]  r16;
    reg         use_crc;

    wire [10:0] K1024 = {r16, 6'b0};                 // 1024R = 64·r16
    wire [10:0] KmL   = K1024 - 11'd24;              // 满块消息容量 Kcb-24
    wire [6:0]  Uu    = {(r16-5'd1), 2'b0};          // U = 64(R-1/16) = 4(r16-1)
    wire [10:0] U16   = {(r16-5'd1), 6'b0};          // 16U = 64(r16-1)
    // 类型2 门限/常量 (r16 为偶数, 904R=56.5·r16 为整数)
    wire [15:0] r16w    = {11'b0, r16};
    wire [15:0] thr1920 = (r16w << 7) - (r16w << 3);                       // 1920R=120·r16
    wire [15:0] r904    = (r16w << 5) + (r16w << 4) + (r16w << 3) + (r16w >> 1); // 904R
    wire [15:0] r128w   = (r16w << 3);                                     // 128R=8·r16

    // 分段迭代
    reg  [15:0] remaining, tmpx;
    reg  [10:0] Kr;
    reg  [4:0]  qv;
    reg  [3:0]  subsel;                              // {512,256,128,64}
    // 类型2 计数
    reg  [3:0]  c1024;
    reg  [1:0]  c512, c256, c128;
    reg  [5:0]  c64;
    reg  [15:0] km;
    reg  [3:0]  idxA;
    reg  signed [15:0] remA;
    // 当前块
    reg  [10:0] Kblk;
    reg  [3:0]  nlog_blk;
    reg         blk_is_last;

    wire [15:0] avail = B_l - mp;                    // 类型2: 剩余可取消息位

    // ---------------- CRC24B (6.9.1.1 / 6.10.1) ----------------
    // 块虚拟序列 V = 0^padv ‖ 消息(Kmsg_cur位) ‖ CRC24(crc_on 时); 按 vpos 顺序消费。
    // 消息位随消费同步喂入 CRC (crc_en), 消费到 CRC 区时余数已就绪 —— 无需旧版的
    // 末块预扫描 (类型3/4 末块细分跨多个子块时 vpos/CRC 跨子块连续, 语义不变)。
    reg  [10:0] padv;                                // V 前补0 位数
    reg  [10:0] Kmsg_cur;                            // V 中消息位数
    reg         crc_on;                              // V 尾部是否有 24 位块 CRC
    reg  [10:0] vpos;                                // V 消费位置

    wire        crc_load = (state == SB_FULL_INIT) || (state == SB_LAST_INIT);
    wire        crc_en, crc_din;
    wire [23:0] crc_val;
    crc_gen #(.WIDTH(24), .POLY(24'hB2B117)) u_crc (
        .clk(clk), .rst_n(rst_n), .load(crc_load), .seed(crc_seed),
        .en(crc_en), .din(crc_din), .crc(crc_val));
    wire [23:0] crc_rev;                             // p_i = crc[23-i] (高位先)
    genvar gc;
    generate for (gc=0; gc<24; gc=gc+1) begin: g_rev
        assign crc_rev[gc] = crc_val[23-gc];
    end endgenerate

    wire [11:0] vend   = {1'b0, padv} + {1'b0, Kmsg_cur};   // 消息区末界 (V 坐标)
    wire        in_pad = (vpos <  padv);
    wire        in_msg = (vpos >= padv) && ({1'b0, vpos} < vend);
    /* verilator lint_off UNUSEDSIGNAL */            // vgap 仅用 [4:0] (CRC 偏移 0..23), 高位不参与
    wire [10:0] vgap   = vpos - vend[10:0];          // CRC 区内: 0..23
    /* verilator lint_on UNUSEDSIGNAL */
    wire        vbit   = in_pad ? 1'b0 : (in_msg ? msg_sh[0] : crc_rev[vgap[4:0]]);

    // ---------------- 冻结集 (6.9.1.4, 附录C 可靠度序列) ----------------
    reg         fg_start;
    /* verilator lint_off UNUSEDSIGNAL */            // fg_busy 观测用, FSM 以 done 驱动
    wire        fg_busy, fg_done;
    /* verilator lint_on UNUSEDSIGNAL */
    wire [NMAX-1:0] fg_frozen;
    frozen_gen #(.NMAX(NMAX)) u_fg (
        .clk(clk), .rst_n(rst_n), .start(fg_start),
        .n_log(nlog_blk), .K(Kblk), .busy(fg_busy), .done(fg_done), .frozen(fg_frozen));

    // ---------------- u 串行装配 (信息位散布, 零组合散布网络) ----------------
    reg [NMAX-1:0] fz_sh;                  // 冻结掩码移位寄存器 (每拍看 [0])
    reg [NMAX-1:0] u_sh;                   // u 移位装配: N 拍后块占据高 N 位
    reg [10:0]     acnt;
    wire [10:0] Nv_blk = 11'd1 << nlog_blk;

    wire        cur_fz = fz_sh[0];
    wire        ubit   = cur_fz ? 1'b0 : vbit;

    assign crc_en  = (state == S_ASM) && !cur_fz && in_msg && crc_on;
    assign crc_din = msg_sh[0];

    // ---------------- 极化编码 (6.9.1.4): 单个纯蝶形 ----------------
    // u_sh 装配后块占据"高 N 位" (u[0] 位于 1024-N); 蝶形要求"低位对齐",
    //   故用 5:1 mux 把高 N 位窗口搬到低 N 位 (nlog_blk 仅 5 种取值, 廉价 5 选 1)。
    wire [NMAX-1:0] u_bottom = (nlog_blk == 4'd9) ? {512'h0, u_sh[1023:512]} :
                               (nlog_blk == 4'd8) ? {768'h0, u_sh[1023:768]} :
                               (nlog_blk == 4'd7) ? {896'h0, u_sh[1023:896]} :
                               (nlog_blk == 4'd6) ? {960'h0, u_sh[1023:960]} :
                                                    u_sh;                   // N=1024
    reg             bf_start;
    wire            bf_done;
    wire [NMAX-1:0] bf_d;
    /* verilator lint_off PINCONNECTEMPTY */          // busy 观测口, FSM 以 done 驱动
    polar_butterfly #(.NMAX(NMAX)) u_bf (
        .clk(clk), .rst_n(rst_n), .start(bf_start),
        .n_log(nlog_blk), .u(u_bottom), .busy(), .done(bf_done), .d(bf_d));
    /* verilator lint_on PINCONNECTEMPTY */

    // ---------------- 码块级联 (6.9.1.5) ----------------
    reg             cat_load, cat_last;
    wire            cat_ready, cat_done;
    wire [10:0]     elen = (11'd1 << nlog_blk);      // E_r = N
    code_block_concat #(.NMAX(NMAX)) u_cat (
        .clk(clk), .rst_n(rst_n),
        .s_load(cat_load), .s_blk(bf_d), .s_elen(elen), .s_last(cat_last),
        .s_ready(cat_ready), .m_valid(m_valid), .m_bit(m_bit),
        .k_count(k_count), .done(cat_done));

    // ---------------- 主 FSM ----------------
    always @(posedge clk or negedge rst_n) begin
        if (!rst_n) begin
            state<=S_IDLE; phase<=PH_BFULL; busy<=0; done<=0; cb_count<=0;
            msg_sh<=0; B_l<=0; mp<=0; r16<=5'd8; use_crc<=0;
            remaining<=0; tmpx<=0; Kr<=0; qv<=0; subsel<=0;
            c1024<=0; c512<=0; c256<=0; c128<=0; c64<=0;
            km<=0; idxA<=0; remA<=0;
            Kblk<=0; nlog_blk<=4'd6; blk_is_last<=0;
            padv<=0; Kmsg_cur<=0; crc_on<=0; vpos<=0;
            fz_sh<=0; u_sh<=0; acnt<=0;
            fg_start<=0; bf_start<=0; cat_load<=0; cat_last<=0;
        end else begin
            done<=0; fg_start<=0; bf_start<=0; cat_load<=0;
            case (state)
                // ================= 入口 =================
                S_IDLE: begin
                    busy<=0;
                    if (start && !busy) begin
                        msg_sh<=msg; B_l<=B; mp<=0; cb_count<=0; busy<=1;
                        use_crc<=0;
                        r16<=tab22_r16(mcs);
                        if (ftype2) begin
                            phase<=PH_A;
                            state<=SA_INIT;
                        end else begin
                            remaining<=B;
                            state<=SB_FULL_INIT;
                        end
                    end
                end

                // ============ 类型3/4: 满块循环 (6.9.1.3) ============
                // 首次判据 B>Kcb (=K1024); 之后判据 remaining>Kcb-24 (满块循环)
                SB_FULL_INIT: begin                  // 本拍同时装载 CRC 种子
                    if (( use_crc && (remaining > {5'b0, KmL}))  ||
                        (!use_crc && (remaining > {5'b0, K1024}))) begin
                        use_crc<=1;
                        Kblk<=K1024; nlog_blk<=4'd10; blk_is_last<=0;
                        padv<=0; Kmsg_cur<=KmL; crc_on<=1; vpos<=0;
                        phase<=PH_BFULL;
                        fg_start<=1; state<=S_FZ;
                    end else begin
                        state<=SB_LAST_INIT;
                    end
                end

                // ============ 类型3/4: 末块 + 细分 ============
                SB_LAST_INIT: begin                  // 本拍装载 CRC 种子 (随消费喂入, 无预扫)
                    Kmsg_cur<=remaining[10:0];
                    Kr<=remaining[10:0] + (use_crc ? 11'd24 : 11'd0);
                    crc_on<=use_crc; vpos<=0;
                    state<=SB_QINIT;
                end
                SB_QINIT: begin                      // 细分决策 (spec 6.9.1.3)
                    phase<=PH_BSUB; subsel<=0;
                    if (r16 == 5'd16) begin          // R=1: 不细分
                        padv<=0; Kblk<=Kr; nlog_blk<=4'd10; blk_is_last<=1;
                        fg_start<=1; state<=S_FZ;
                    end else if (Kr > U16) begin     // Kr>16U: 补0至1024R, 码长1024 @ 码率R
                        //   K = K1024 = 1024R = 表23 "码长1024" 列 (与满块同值)
                        padv<=K1024-Kr; Kblk<=K1024; nlog_blk<=4'd10; blk_is_last<=1;
                        fg_start<=1; state<=S_FZ;
                    end else begin                   // 迭代求 q=⌈Kr/U⌉
                        tmpx<={5'b0,Kr}; qv<=5'd1;
                        state<=SB_QLOOP;
                    end
                end
                SB_QLOOP: begin
                    if (tmpx > {9'b0,Uu}) begin
                        tmpx<=tmpx-{9'b0,Uu}; qv<=qv+1'b1;
                    end else begin                   // 终止: 余量∈(0,U], pad=U-余量
                        padv<={4'b0,Uu}-tmpx[10:0];
                        if (qv == 5'd16) begin       // q=16: 码长1024 @ 码率R-1/16
                            //   K = 1024(R-1/16) = 16U (按码率定义, 非表23格值)
                            Kblk<=U16; nlog_blk<=4'd10; blk_is_last<=1;
                            fg_start<=1; state<=S_FZ;
                        end else begin               // q≤15: 二进制选码长
                            subsel<=qv[3:0];
                            state<=SB_SUBNEXT;
                        end
                    end
                end
                SB_SUBNEXT: begin                    // 512→256→128→64 顺序; K 值查【表23】(码率R-1/16)
                    if (subsel[3]) begin             // 码长512: K = tab23[R,512]
                        nlog_blk<=4'd9; Kblk<={1'b0,tab23(r16,2'd0)};
                        subsel[3]<=0; blk_is_last<=(subsel[2:0]==3'b0);
                    end else if (subsel[2]) begin    // 码长256: K = tab23[R,256]
                        nlog_blk<=4'd8; Kblk<={1'b0,tab23(r16,2'd1)};
                        subsel[2]<=0; blk_is_last<=(subsel[1:0]==2'b0);
                    end else if (subsel[1]) begin    // 码长128: K = tab23[R,128]
                        nlog_blk<=4'd7; Kblk<={1'b0,tab23(r16,2'd2)};
                        subsel[1]<=0; blk_is_last<=(subsel[0]==1'b0);
                    end else begin                   // 码长64:  K = tab23[R,64]
                        nlog_blk<=4'd6; Kblk<={1'b0,tab23(r16,2'd3)};
                        subsel[0]<=0; blk_is_last<=1;
                    end
                    fg_start<=1; state<=S_FZ;
                end

                // ============ 类型2: 计数 (6.9.1.2 查表) ============
                SA_INIT: begin
                    c1024<=0; km<=B_l;
                    if (B_l > thr1920) begin         // N1024 = ⌊(B-904R)/1024R⌋
                        tmpx<=B_l - r904;
                        state<=SA_N1024;
                    end else begin
                        tmpx<=B_l - 16'd1;           // (Km-1)
                        idxA<=0;
                        state<=SA_IDX;
                    end
                end
                SA_N1024: begin
                    // 循环: tmpx=(B-904R)-n·1024R 与 km=B-n·1024R 同步递减;
                    // 终止时 n=⌊(B-904R)/1024R⌋=N1024, km 即剩余待编码比特 Km
                    if (tmpx >= {5'b0,K1024}) begin
                        tmpx<=tmpx-{5'b0,K1024};
                        km<=km-{5'b0,K1024};
                        c1024<=c1024+1'b1;
                    end else begin
                        tmpx<=km-16'd1;              // 转 (Km-1) 供 index 迭代
                        idxA<=0;
                        state<=SA_IDX;
                    end
                end
                SA_IDX: begin                        // index=⌊(Km-1)/128R⌋, 钳位14
                    if ((tmpx >= r128w) && (idxA < 4'd14)) begin
                        tmpx<=tmpx-r128w; idxA<=idxA+1'b1;
                    end else begin
                        {c512,c256,c128}<=tab20(idxA);
                        state<=SA_REM;
                    end
                end
                SA_REM: begin                        // rem = Km - Σ N·K_N (表24)
                    remA <= $signed({1'b0,km[14:0]})
                          - $signed({5'b0,mul012(c512,tab24(r16,2'd0))})
                          - $signed({5'b0,mul012(c256,tab24(r16,2'd1))})
                          - $signed({5'b0,mul012(c128,tab24(r16,2'd2))});
                    c64<=0;
                    state<=SA_N64;
                end
                SA_N64: begin                        // N64=⌈rem/K64⌉ (迭代)
                    if ((remA > 0) && (tab24(r16,2'd3) != 10'd0)) begin
                        remA<=remA - $signed({5'b0,{1'b0,tab24(r16,2'd3)}});
                        c64<=c64+1'b1;
                    end else
                        state<=SA_BLKNEXT;
                end
                SA_BLKNEXT: begin                    // 块序 1024→512→256→128→64
                    if (c1024 != 0) begin
                        nlog_blk<=4'd10; Kblk<=K1024; c1024<=c1024-1'b1;
                        blk_is_last<=((c1024==4'd1)&&(c512==0)&&(c256==0)&&(c128==0)&&(c64==0));
                    end else if (c512 != 0) begin
                        nlog_blk<=4'd9; Kblk<={1'b0,tab24(r16,2'd0)}; c512<=c512-1'b1;
                        blk_is_last<=((c512==2'd1)&&(c256==0)&&(c128==0)&&(c64==0));
                    end else if (c256 != 0) begin
                        nlog_blk<=4'd8; Kblk<={1'b0,tab24(r16,2'd1)}; c256<=c256-1'b1;
                        blk_is_last<=((c256==2'd1)&&(c128==0)&&(c64==0));
                    end else if (c128 != 0) begin
                        nlog_blk<=4'd7; Kblk<={1'b0,tab24(r16,2'd2)}; c128<=c128-1'b1;
                        blk_is_last<=((c128==2'd1)&&(c64==0));
                    end else begin
                        nlog_blk<=4'd6; Kblk<={1'b0,tab24(r16,2'd3)}; c64<=c64-1'b1;
                        blk_is_last<=(c64==6'd1);
                    end
                    state<=SA_PAD;
                end
                SA_PAD: begin                        // 末块不足位: 块首补0
                    padv     <= (avail < {5'b0,Kblk}) ? (Kblk - avail[10:0]) : 11'd0;
                    Kmsg_cur <= (avail < {5'b0,Kblk}) ? avail[10:0] : Kblk;
                    crc_on<=0; vpos<=0;
                    fg_start<=1; state<=S_FZ;
                end

                // ============ 公共: 冻结集 → 装配 → 编码 → 级联 → 调度 ============
                S_FZ: begin                          // 冻结集生成 → 装入移位寄存器
                    if (fg_done) begin
                        fz_sh<=fg_frozen; acnt<=0;
                        state<=S_ASM;
                    end
                end
                S_ASM: begin                         // u 串行装配 (N 拍); 消息位同步喂 CRC
                    u_sh<={ubit, u_sh[NMAX-1:1]};
                    fz_sh<={1'b0, fz_sh[NMAX-1:1]};
                    if (!cur_fz) begin
                        vpos<=vpos+11'd1;
                        if (in_msg) begin
                            msg_sh<={1'b0, msg_sh[MSG_MAX-1:1]};
                            mp<=mp+16'd1;
                        end
                    end
                    if (acnt == Nv_blk - 11'd1) begin
                        bf_start<=1;                 // 启动蝶形 (u_bottom 已就绪)
                        state<=S_ENC;
                    end else
                        acnt<=acnt+11'd1;
                end
                S_ENC: begin                         // 蝶形编码 (log2(N)+1 拍)
                    if (bf_done)
                        state<=S_CATW;
                end
                S_CATW: begin
                    if (cat_ready) begin
                        cat_load<=1; cat_last<=blk_is_last;
                        cb_count<=cb_count+1'b1;
                        state<=S_ROUTE;
                    end
                end
                S_ROUTE: begin
                    case (phase)
                        PH_BFULL: begin
                            remaining<=remaining-{5'b0,KmL};
                            state<=SB_FULL_INIT;
                        end
                        PH_BSUB:  state<=(subsel!=4'b0) ? SB_SUBNEXT : S_DONE;
                        default:  state<=({c1024,c512,c256,c128,c64}!=0)
                                          ? SA_BLKNEXT : S_DONE;
                    endcase
                end
                S_DONE: begin
                    if (cat_done) begin
                        done<=1; busy<=0; state<=S_IDLE;
                    end
                end
                default: state<=S_IDLE;
            endcase
        end
    end
endmodule

---

### ChatGPT

我先把这个顶层反向拆解成“译码端必须镜像的流程”，同时标出目前还缺的接口和最容易出现位序错误的地方。等你把内部模块发来后，我们再把 SC 核心嵌进去。

明白。这个顶层已经足以确定 SC 译码链的总体框架。译码端不能只写一个 SC decoder，还要完整反向恢复编码端的：
码块划分 → 冻结集生成 → SC 译码 → 信息位抽取 → 去补零 → CRC 校验 → 消息拼接。
一、译码端整体结构
建议译码顶层设计为：
Plain text串行接收 LLR      │      ▼码块规划器根据 B、mcs、ftype2重新计算每个码块的 N、K、是否末块、补零数      │      ▼码块 LLR 缓存收满 N 个软信息      │      ▼frozen_gen生成与编码端完全相同的冻结位置      │      ▼SC 译码核心输出 u_hat[0:N-1]      │      ▼非冻结位抽取恢复虚拟序列 V      │      ▼去前置补零 padv      │      ▼消息位 / CRC24B 分离      │      ▼CRC 校验及多码块消息拼接
其中真正的 SC 算法只负责：
Plain textLLR[N] + frozen[N] → u_hat[N]
而 B、MCS、码块划分、CRC、补零 应当放在 SC 核心外部。

二、译码端必须镜像的三种编码情形
1. 类型 3/4 的普通单块
当消息不需要分块时：
Plain textV = 消息
或者在需要补零时：
Plain textV = 0^padv || 消息
SC 译码完成后，按照冻结集抽取所有非冻结位置，就能得到 V。

2. 类型 3/4 的 CRC 分块
当：
verilogB > K1024
编码端启用了 CRC，每个逻辑码块是：
Plain text消息 Kcb-24 位 || CRC24B
需要特别注意：末尾逻辑码块可能又被拆成若干个长度为：
Plain text512、256、128、64
的物理 Polar 子块。
因此译码端不能对每个物理子块分别做 CRC。正确流程是：
Plain textSC 解第一个物理子块    ↓抽取非冻结位    ↓SC 解第二个物理子块    ↓继续拼接非冻结位    ↓获得完整逻辑码块 V    ↓再检查末尾 24 位 CRC
也就是说：

一个 CRC 可能跨越多个 Polar 子块。

这一点是译码控制逻辑中最容易写错的地方。

3. 类型 2
类型 2 没有 CRC：
verilogcrc_on <= 0;
末块不足 Kblk 时，在信息序列前面补零：
Plain textV = 0^padv || 剩余消息
这些补零并不是冻结位，而是被放入了正常的信息位位置。
因此译码后需要：


抽取全部非冻结位；


丢弃前 padv 位；


保留后面的消息位；


最好检查前 padv 位是否全部译为 0，作为辅助错误检测。



三、SC 输入建议使用 LLR，而不是硬比特
目前编码顶层输出：
verilogm_validm_bit
但接收端的 SC 译码器最好输入软信息：
veriloginput wire signed [LLR_W-1:0] s_llr;
约定：
Plain textLLR > 0：更倾向于比特 0LLR < 0：更倾向于比特 1
若当前只能进行无噪声环回仿真，可以把硬比特转换成固定幅度 LLR：
verilogassign llr_in = rx_bit ? -LLR_MAG : LLR_MAG;
例如：
veriloglocalparam signed [7:0] LLR_MAG = 8'sd32;
但正式通信系统中直接使用硬比特会明显损失译码性能。

四、SC 核心的基本计算
SC 译码主要使用两个函数。
f 函数
推荐先使用 min-sum：
Plain textf(a,b) = sign(a) × sign(b) × min(|a|,|b|)
对应硬件逻辑大致为：
verilogf_out = (a_sign ^ b_sign) ? -min_abs : min_abs;
g 函数
Plain textg(a,b,u) = b + (1-2u)a
即：
verilogg_out = u ? (b-a) : (b+a);
叶节点判决：
verilogif (frozen[i])    u_hat[i] = 0;else    u_hat[i] = (leaf_llr < 0);
内部 LLR 位宽需要逐层扩展或做饱和处理，避免 g() 加减溢出。例如输入 8 位 LLR，可以把内部运算宽度设置为 10～12 位。

五、当前顶层中发现的一个配置风险
类型 2 的 tab24() 仅定义了：
Plain textR = 5/8R = 3/4R = 7/8
也就是：
verilogr16 = 10、12、14
但顶层并未阻止类型 2 使用其他 MCS。
如果 ftype2=1，同时 r16 不是 10、12、14，那么：
verilogtab24(...) = 0
随后 SA_BLKNEXT 仍可能进入 64 位码块分支，出现：
verilogKblk = 0;c64  = c64 - 1;
从而发生计数下溢和错误调度。
编码端和译码端都建议增加合法性判断：
verilogwire type2_rate_valid =    (r16 == 5'd10) ||    (r16 == 5'd12) ||    (r16 == 5'd14);
非法时直接置位：
verilogcfg_error <= 1'b1;busy      <= 1'b0;done      <= 1'b1;
否则编码器和译码器可能以不同的错误状态运行。

六、建议把码块规划逻辑独立出来
现在码块规划全部写在 polar_encode_chain 状态机里。如果译码器再复制一份，后续修改表格或边界条件时，很容易造成编码和译码不一致。
建议提取公共模块：
verilogpolar_block_plan
输入：
verilogBmcsftype2start
逐块输出：
verilogblk_validblk_readyn_logKpadvKmsgcrc_onlogic_block_lastframe_last
编码器和译码器共同使用这个模块。
其中应区分两个“末尾”概念：
Plain textlogic_block_last：当前物理 Polar 子块是否为一个 CRC 逻辑块的最后一个子块frame_last：是否为整个消息的最后一个 Polar 子块
现在编码端只有：
verilogblk_is_last
它实际主要服务于码块级联的整帧结束。译码器由于还要处理跨物理子块 CRC，最好增加逻辑块结束标志。

七、建议的译码顶层接口
后续可以按下面的形式实现：
verilogmodule polar_decode_chain #(    parameter integer NMAX    = 1024,    parameter integer MSG_MAX = 2048,    parameter integer LLR_W   = 8,    parameter integer INT_W   = 12)(    input  wire                         clk,    input  wire                         rst_n,    input  wire                         start,    input  wire [15:0]                  B,    input  wire [3:0]                   mcs,    input  wire                         ftype2,    input  wire [23:0]                  crc_seed,    input  wire                         s_valid,    input  wire signed [LLR_W-1:0]      s_llr,    output wire                         s_ready,    output reg  [MSG_MAX-1:0]           msg,    output reg  [15:0]                  msg_len,    output reg                          crc_ok,    output reg                          decode_error,    output reg                          busy,    output reg                          done,    output reg  [7:0]                   cb_count);
若希望节省资源，也可以把消息改成串行输出：
verilogoutput wire msg_valid;output wire msg_bit;output wire msg_last;
不过为了和当前编码顶层直接环回验证，第一版使用并行：
verilogoutput reg [MSG_MAX-1:0] msg
会更方便。

八、接下来最关键的内部模块
下一步最好依次提供：


polar_butterfly


frozen_gen


code_block_concat


crc_gen


其中最先需要看的是 polar_butterfly。
因为必须确认当前编码采用的是：
Plain textx = u·F⊗n
还是包含比特反转的：
Plain textx = u·B_N·F⊗n
同时还要确认：
Plain textbf_d[0]
究竟是不是最先发送的 x[0]。这会直接决定 SC 树的叶节点顺序、LLR 缓存顺序以及最终 u_hat 的索引。只要这里差一个比特反转，即使 SC 算法本身完全正确，译码结果也会全部错位。

---

### User

1.我们只负责图片中B的部分，你顺便解释一下PE是什么意思以及括号里的（f/g）
2.类型二真的没有CRC吗，只是每个码块没带，但是总体还是带的吧
3.是使用LLR，但是应该不是只进行无噪声环回仿真，虽然我也不清楚这个是什么，你顺便解释一下
4.先用并行的吧
5. 
module polar_butterfly #(
    parameter integer NMAX = 1024
)(
    input  wire             clk,
    input  wire             rst_n,
    input  wire             start,
    input  wire [3:0]       n_log,      // log2(N)
    input  wire [NMAX-1:0]  u,          // 预装配 u (低 N 位有效, u[k]=第 k 个输入, 高位0)
    output reg              busy,
    output reg              done,
    output reg [NMAX-1:0]   d           // 码字 (低 N 位有效)
);
    localparam integer SMAX = $clog2(NMAX);
    localparam integer CW   = (SMAX <= 1) ? 1 : $clog2(SMAX+1);

    reg  [NMAX-1:0] x;
    reg  [CW-1:0]   stage;
    wire [NMAX-1:0] stage_net [0:SMAX-1];

    genvar gs, gi, gj;
    generate
        for (gs = 0; gs < SMAX; gs = gs + 1) begin : g_stage
            localparam integer M = (1 << gs);
            for (gi = 0; gi < NMAX; gi = gi + 2*M) begin : g_grp
                for (gj = 0; gj < M; gj = gj + 1) begin : g_pair
                    assign stage_net[gs][gi+gj]   = x[gi+gj] ^ x[gi+gj+M];
                    assign stage_net[gs][gi+gj+M] = x[gi+gj+M];
                end
            end
        end
    endgenerate

    wire [NMAX-1:0] x_next  = stage_net[stage];
    wire [CW-1:0]   last_st = n_log - 1'b1;

    always @(posedge clk or negedge rst_n) begin
        if (!rst_n) begin
            busy  <= 1'b0;
            done  <= 1'b0;
            stage <= {CW{1'b0}};
            x     <= {NMAX{1'b0}};
            d     <= {NMAX{1'b0}};
        end else begin
            done <= 1'b0;
            if (start && !busy) begin
                x     <= u;
                stage <= {CW{1'b0}};
                busy  <= 1'b1;
            end else if (busy) begin
                x <= x_next;
                if (stage == last_st) begin
                    d    <= x_next;
                    busy <= 1'b0;
                    done <= 1'b1;
                end else begin
                    stage <= stage + 1'b1;
                end
            end
        end
    end
endmodule
module polar_encoder #(
    parameter integer N = 64
)(
    input  wire         clk,
    input  wire         rst_n,      // 异步低有效复位
    // 控制握手
    input  wire         start,      // busy=0 时的 start 被接受
    output reg          busy,
    output reg          done,       // 单拍脉冲, 表示 d 已更新为本次码字
    // 数据
    input  wire [N-1:0] frozen,     // 冻结掩码: 1=冻结位(u=0), 0=信息位
    input  wire [N-1:0] info,       // 信息比特, 右对齐: info[0]=c0 ... 共 K 个有效
    output reg  [N-1:0] d           // 码字 d = u * G_N
);
    localparam integer STAGES = $clog2(N);
    localparam integer SW     = (STAGES <= 1) ? 1 : $clog2(STAGES);

    // ---- u 的装配 (spec 伪代码): 冻结位置 0, 信息位顺序填入 info ----
    // 注: 对大 N 这是一个较大的组合散布网络, 仅在装载拍使用;
    //     若综合后时序紧张, 可将其拆为独立流水级.
    reg [N-1:0] u_packed;
    integer n, k;
    always @* begin
        k = 0;
        u_packed = {N{1'b0}};
        for (n = 0; n < N; n = n + 1) begin
            if (!frozen[n]) begin
                u_packed[n] = info[k];
                k = k + 1;
            end
        end
    end

    // ---- log2(N) 套单级蝶形网络, 由 stage 计数器选择当前应用哪一级 ----
    reg  [N-1:0]  x;                        // 状态寄存器 (原位变换)
    reg  [SW-1:0] stage;
    wire [N-1:0]  stage_net [0:STAGES-1];   // 第 s 级网络作用于当前 x 的结果

    genvar gs, gi, gj;
    generate
        for (gs = 0; gs < STAGES; gs = gs + 1) begin : g_stage
            localparam integer M = (1 << gs);
            for (gi = 0; gi < N; gi = gi + 2*M) begin : g_grp
                for (gj = 0; gj < M; gj = gj + 1) begin : g_pair
                    assign stage_net[gs][gi+gj]   = x[gi+gj] ^ x[gi+gj+M]; // 顶半异或
                    assign stage_net[gs][gi+gj+M] = x[gi+gj+M];            // 底半直传
                end
            end
        end
    endgenerate

    wire [N-1:0] x_next = stage_net[stage];

    // ---- 控制 FSM ----
    always @(posedge clk or negedge rst_n) begin
        if (!rst_n) begin
            busy  <= 1'b0;
            done  <= 1'b0;
            stage <= {SW{1'b0}};
            x     <= {N{1'b0}};
            d     <= {N{1'b0}};
        end else begin
            done <= 1'b0;
            if (start && !busy) begin              // 接受任务: 装载 u
                x     <= u_packed;
                stage <= {SW{1'b0}};
                busy  <= 1'b1;
            end else if (busy) begin
                x <= x_next;                       // 应用第 stage 级蝶形
                if (stage == STAGES-1) begin       // 最后一级: 输出码字
                    d    <= x_next;
                    busy <= 1'b0;
                    done <= 1'b1;
                end else begin
                    stage <= stage + 1'b1;
                end
            end
        end
    end

---

### ChatGPT

可以，我们只负责图中的 B：SC 内核数据通路。也就是说，暂时不负责码块拆分、CRC、冻结集生成和整个译码链状态机，只实现：
Plain text输入一个码块的 N 个 LLR + frozen mask                  ↓             SC 数据通路                  ↓输出并行的 û[0:N-1]

1. PE 是什么？PE(f/g) 是什么意思？
PE 是 Processing Element，处理单元。
在 SC 译码中，PE 的主要任务就是执行两种 LLR 运算：
Plain textf 运算：向译码树左子节点传播 LLRg 运算：向译码树右子节点传播 LLR
所以表格里的：
Plain textPE(f/g)
表示一个能够根据控制信号选择执行 f 或 g 的计算单元。
f 运算
精确的 LLR 形式为：
$f(a,b)=2\operatorname{atanh}
\left(
\tanh(a/2)\tanh(b/2)
\right)$
硬件通常不用这个精确公式，而采用 min-sum 近似：
$f(a,b)
=
\operatorname{sign}(a)
\operatorname{sign}(b)
\min(|a|,|b|)$
Verilog 逻辑可以写成：
verilogabs_a  = a[W-1] ? -a : a;abs_b  = b[W-1] ? -b : b;min_ab = (abs_a < abs_b) ? abs_a : abs_b;f_out = a[W-1] ^ b[W-1] ? -min_ab : min_ab;
它的直观意义是：


a、b 符号相同，结果倾向于 0；


a、b 符号不同，结果倾向于 1；


可靠度取两者中较弱的一个。


g 运算
$g(a,b,\beta)=b+(1-2\beta)a$
等价于：
verilogg_out = beta ? (b - a) : (b + a);
这里的 beta 不是当前待判决位，而是已经译出的左子树部分和。
因此：
Plain textbeta = 0：g = b + abeta = 1：g = b - a
一个 PE 的基本接口
后续可以设计成：
verilogmodule sc_pe #(    parameter integer LLR_W = 8)(    input  wire signed [LLR_W-1:0] a,    input  wire signed [LLR_W-1:0] b,    input  wire                    mode_g, // 0=f，1=g    input  wire                    beta,    output wire signed [LLR_W:0]   y);
注意输出通常比输入多一位，因为 g 中存在加减法。

2. 类型二到底有没有 CRC？
这里需要区分两种 CRC：
Polar 码块 CRC
这是当前编码模块内部添加的：
Plain text每个逻辑码块消息 || CRC24B
从你提供的代码看，类型 2 明确没有添加这种码块 CRC：
verilogSA_PAD: begin    ...    crc_on <= 0;end
所以可以确定：

类型 2 的每个 Polar 码块不携带此模块生成的 CRC24B。

上层整体 CRC
但是，这不代表整个无线帧或整个业务数据一定没有 CRC。
很可能在进入 polar_encode_chain 之前，上层已经完成了：
Plain text原始业务数据    ↓帧级 CRC / MAC CRC / 传输块 CRC    ↓得到 msg 和 B    ↓进入 Polar 编码
这时，对 polar_encode_chain 来说，那个整体 CRC 只是普通的消息比特，它不会通过这里的：
verilogcrc_gen
再次生成。
因此你的理解可能是对的：

类型 2 没有“每个 Polar 码块的 CRC24B”，但是输入消息整体可能已经包含了上层加入的 CRC。

不过仅凭当前顶层代码，只能确认“本模块没有给类型 2 添加 CRC”，无法确认 msg 输入之前是否已经带了总体 CRC。这个要看编码链的上一级模块或者标准中进入 6.9.1.2 之前的处理过程。
对我们负责的 B 模块而言，CRC 不影响 SC 内核。SC 内核仍然只输出：
Plain text所有非冻结位的估计结果
CRC 检查属于图片中的 D 外层链路。

3. 什么是“无噪声环回仿真”？
无噪声环回是最基础的功能验证。
编码器输出：
Plain text0 或 1
不经过真实信道、调制噪声和干扰，而是直接转换成一个非常可靠的 LLR，再送给译码器。
例如约定：
Plain textLLR > 0 表示更可能是 0LLR < 0 表示更可能是 1
可以转换为：
verilogassign llr_in = encoded_bit ? -8'sd32 : 8'sd32;
整个环回过程是：
Plain text原始消息   ↓Polar 编码   ↓码字比特   ↓理想硬比特转 LLR   ↓SC 译码   ↓恢复消息
这里没有加入随机噪声，所以称为无噪声环回。
它主要用于检查：


编码和译码位序是否一致；


frozen mask 是否一致；


SC 的 f/g 顺序是否正确；


部分和是否正确；


输出 u_hat 是否和编码输入 u 一致。


只有无噪声环回完全通过，才能继续做带噪声仿真。
带噪声仿真
更真实的验证一般是：
Plain textPolar 编码   ↓BPSK 调制0 → +11 → -1   ↓AWGN 高斯白噪声信道   ↓接收值 y   ↓计算 LLR   ↓SC 译码
对于 BPSK + AWGN：
$LLR=\frac{2y}{\sigma^2}$
然后统计：
Plain textBER：比特错误率BLER：码块错误率FER：帧错误率
所以你们正式系统确实应该使用真实或量化后的软 LLR，而不仅仅是硬比特环回。
不过开发顺序通常还是：
Plain text第一步：无噪声环回，验证逻辑正确第二步：固定错误注入第三步：AWGN 软件仿真第四步：定点位宽扫描第五步：RTL 联合仿真

4. 并行输出可以
第一版 SC 内核可以直接输出：
verilogoutput reg [NMAX-1:0] u_hat;
低 N 位有效，并约定：
Plain textu_hat[k] = 第 k 个 Polar 输入位的译码结果
这和编码器的：
verilogu[k] = 第 k 个输入
保持一致。
外层模块 D 再根据：
verilogfrozen[k]
抽取信息位：
verilogif (!frozen[k])    info_hat[j] = u_hat[k];
虽然 SC 是串行逐位判决的，但是内部可以边判决边写入寄存器，最后一次性并行输出。

5. 你提供的蝶形编码器使用了什么位序？
你的编码网络是：
verilogassign stage_net[gs][gi+gj]    = x[gi+gj] ^ x[gi+gj+M];assign stage_net[gs][gi+gj+M]    = x[gi+gj+M];
并且级间距离依次为：
Plain textM = 1、2、4、8……N/2
以 N=4 为例。
输入：
Plain textu0 u1 u2 u3
第 0 级：
Plain textu0⊕u1, u1, u2⊕u3, u3
第 1 级：
Plain textu0⊕u1⊕u2⊕u3,u1⊕u3,u2⊕u3,u3
所以输出为：
$d=
[
u_0\oplus u_1\oplus u_2\oplus u_3,\ 
u_1\oplus u_3,\ 
u_2\oplus u_3,\ 
u_3
]$
这对应：
$d=uF^{\otimes n}$
当前编码模块内部没有显式执行比特反转 B_N。
因此我们设计 SC 核心时，应当采用与：
$F^{\otimes n}$
对应的自然索引顺序，不应额外把输入 LLR 或输出 u_hat 做位反转。
前提是外部串行发送顺序确实是：
Plain textd[0]、d[1]、d[2]……
code_block_concat 发来后，还要最后确认这一点。

我们负责的 B 模块边界
为了不和 C、D 重叠，建议将 B 模块接口固定为：
verilogmodule polar_sc_core #(    parameter integer NMAX  = 1024,    parameter integer LLR_W = 8,    parameter integer INT_W = 12)(    input  wire                         clk,    input  wire                         rst_n,    input  wire                         start,    input  wire [3:0]                   n_log,    // 一个码块的并行输入 LLR，低 N 项有效    input  wire signed [NMAX*LLR_W-1:0] llr_in,    // 低 N 位有效，1=冻结，0=信息位    input  wire [NMAX-1:0]              frozen,    output reg                          busy,    output reg                          done,    // 并行译码结果，低 N 位有效    output reg [NMAX-1:0]               u_hat);
不过 Verilog 中将二维 LLR 压成：
verilog[NMAX*LLR_W-1:0]
可读性较差。若使用 SystemVerilog，建议直接写：
systemveriloginput logic signed [LLR_W-1:0] llr_in [0:NMAX-1];
如果项目必须使用 Verilog-2001，也可以采用串行装载 LLR：
veriloginput  wire signed [LLR_W-1:0] s_llr;input  wire                    s_valid;output wire                    s_ready;
但这会让 LLR 装载控制和 B 内核耦合。既然你目前决定先用并行，第一版可以直接用扁平化总线。

B 模块内部包含什么
我们的 SC 内核建议拆成三个子模块：
Plain textpolar_sc_core├── sc_pe│   └── f/g 定点运算├── sc_llr_memory│   └── 保存各层 LLR α└── sc_partial_sum    └── 保存判决位和部分和 β
B 负责的具体内容是：
LLR 数据 α
保存和更新 SC 树上的 LLR。
Plain text根节点：信道输入 LLR左子节点：f(a,b)右子节点：g(a,b,beta)
部分和 β
部分和来自已经完成的硬判决。
父节点部分和更新关系一般为：
$\beta_{\text{left,parent}}
=
\beta_L\oplus\beta_R$
$\beta_{\text{right,parent}}
=
\beta_R$
这和编码蝶形的异或关系相对应。
叶节点判决
到达叶节点 phi 时：
verilogif (frozen[phi])    u_hat[phi] <= 1'b0;else    u_hat[phi] <= leaf_llr[INT_W-1];
在我们约定：
Plain text正 LLR → 0负 LLR → 1
的情况下，符号位就是硬判决。

B 和 C 的责任边界
虽然表格中写了：
Plain textB：PE、LLR 存储、部分和网络C：产生 f/g 顺序、存储读写时序、frozen 时点
实际编码时，B 模块需要暴露类似控制接口：
verilogpe_mode_gpe_ape_bpe_betape_yllr_rd_addrllr_wr_addrllr_wr_enbeta_rd_addrbeta_wr_addrbeta_wr_en
然后由 C 模块决定每一拍：
Plain text读哪个 LLR执行 f 还是 g结果写到哪里何时进行叶节点判决何时更新部分和
不过为了降低联调复杂度，也可以把 B+C 暂时实现为一个完整的 polar_sc_core，等功能跑通后再按任务边界拆分。否则纯 B 数据通路没有调度控制，单独几乎无法仿真验证。
从分工角度，可以把交付物定义为：
Plain textB 提供：1. sc_pe2. LLR memory3. partial-sum memory/network4. 数据通路顶层及控制接口5. 定点浮点对拍模型C 提供：1. SC 深度优先遍历调度2. f/g 控制3. 地址生成4. 判决和部分和更新时序
你们只负责 B 的话，下一步最合适的是先确定 LLR 位宽、PE 并行度和存储架构，然后实现 sc_pe 和浮点/定点对拍模型。

---

### User

1.LLR应该是要串行装载的，那输出也用串行的吧
2.LLR 位宽、PE 并行度和存储架构，这部分在哪，代码还是协议里
3.纯B没办法单独跑仿真，但是是可以单独写的吧

---

### ChatGPT

对，输入和输出都改成串行更合理，但要区分：


接口串行：每拍装入一个 LLR、每拍输出一个译码比特；


内部 PE 并行度：同一拍内部同时计算多少组 f/g。


两者并不矛盾。即使输入每拍只有一个 LLR，内部仍然可以使用 1、2、4、8 个 PE 并行计算。

1. 建议的串行接口
LLR 串行装载
一个码块开始后，连续输入 $N$ 个 LLR：
veriloginput  wire                         llr_valid;input  wire signed [LLR_W-1:0]      llr_in;output wire                         llr_ready;
约定装载顺序：
Plain text第0个有效LLR → d[0]第1个有效LLR → d[1]……第N-1个有效LLR → d[N-1]
这与当前编码器自然顺序保持一致，不做比特反转。
建议增加：
veriloginput wire llr_last;
但它不是必需的，因为已知：
verilogN = 1 << n_log;
内部可以自己计数。第一版用计数器判断收满即可，避免 llr_last 与计数不一致。

译码结果串行输出
建议不要在叶节点刚判决时立即向外输出。因为 SC 译码中，两次叶节点判决之间可能隔着若干拍 f/g 和部分和更新，输出的 u_valid 会断断续续。
更清晰的方式是：


SC 译码过程中把判决写入 u_mem；


全部 $N$ 位译码结束；


再连续串行输出 u_hat[0] 到 u_hat[N-1]。


接口可以写成：
verilogoutput wire                    u_valid;output wire                    u_bit;output wire [10:0]             u_index;output wire                    u_last;input  wire                    u_ready;
这里建议输出的是所有 $N$ 个 u_hat：
Plain textu_hat[0], u_hat[1], ..., u_hat[N-1]
包括冻结位置。冻结位置正常应为 0。
D 模块再根据：
verilogfrozen[u_index]
提取非冻结位、去补零、做 CRC，而不是让 B 模块直接输出消息比特。

2. LLR 位宽、PE 并行度和存储架构来自哪里？
这三项通常不是协议直接规定的。
它们的来源不同。
协议规定的内容
协议主要规定：


支持的码长 $N$；


信息位数量 $K$；


冻结位置；


Polar 编码关系；


使用 SC 或其他规定译码流程时的数学行为；


码块划分和 CRC 规则。


协议一般不会规定：
Plain textLLR 必须是 8 位必须使用 4 个 PE必须使用 BRAM 或寄存器
这些属于硬件实现选择。

LLR 位宽来自 A 模块的定点扫描
图片中 A 是：
Plain text浮点 SC 参考模型定点位宽扫描生成对拍向量
所以 A 应当给出 B 最终采用的 LLR 位宽。
典型工作过程是：
Plain text浮点 SC 性能作为基准       ↓尝试不同定点格式4 bit、5 bit、6 bit、8 bit……       ↓比较 BER/BLER 性能损失       ↓选择性能和硬件资源折中
例如可能最终确定：
verilogparameter LLR_W = 8;parameter INT_W = 10;
其中：


LLR_W：输入信道 LLR 位宽；


INT_W：内部计算和存储位宽。


g 运算存在加减：
$g(a,b,\beta)=b\pm a$
所以内部位宽通常需要比输入大，或者采用饱和截断。
在 A 还没有完成定点扫描前，B 应当先参数化，而不是把位宽写死：
verilogparameter integer LLR_W = 8;parameter integer INT_W = 10;
第一版建议暂定：
Plain text输入 LLR：8 位有符号内部 LLR：10 位有符号
但这只是开发初始值，最终要由 A 的仿真结果确认。

PE 并行度来自吞吐率和 FPGA 资源指标
PE 并行度不是协议决定的，而是由以下条件决定：


要求的码块译码时间；


工作时钟；


目标吞吐率；


FPGA 的 LUT、寄存器和 BRAM 数量；


是否需要支持连续码块；


C 模块调度复杂度。


可以把并行度参数化：
verilogparameter integer PE_NUM = 1;
含义是每拍同时执行多少组 f/g。
PE_NUM = 1
优点：


结构最简单；


地址控制最简单；


资源最少；


最适合第一版验证。


缺点：


译码周期最多；


N=1024 时延较长。


PE_NUM = 2、4、8
优点：


一拍处理多个 LLR 对；


译码速度提高。


缺点：


需要多端口或分 Bank 存储；


读写地址生成更复杂；


C 模块调度明显更难；


容易发生存储端口冲突。


因此第一版建议：
verilogPE_NUM = 1
先验证正确性，再扩展到多 PE。你们表格里也写了 B 依赖 A 的定点位宽，但没有说必须一开始就高度并行。

存储架构来自 PE_NUM 和 FPGA 资源
存储主要包括两部分：
Plain textα：LLR 存储β：部分和存储
具体用寄存器还是 BRAM，同样不是协议规定。
方案一：全部寄存器
优点：


读写简单；


可组合读取；


适合仿真和初版；


不用处理 BRAM 一拍读延迟。


缺点：


NMAX=1024 时资源较大；


多层 LLR 全展开会消耗较多寄存器。


适合先实现功能正确版本。
方案二：BRAM 存储
优点：


节约 LUT 和寄存器；


适合 N=1024；


更接近最终 FPGA 实现。


缺点：


通常同步读取，有一拍延迟；


双端口数量有限；


PE_NUM 增大后需要分 Bank；


C 调度必须匹配存储延迟。


推荐开发顺序
Plain text第一版：PE_NUM=1寄存器数组建模验证算法和位序第二版：替换成同步 RAM 模型加入一拍读延迟第三版：推断 BRAM / XPM Memory根据时序和资源决定是否多 Bank第四版：提高 PE_NUM
所以“LLR 位宽、PE 并行度和存储架构”最终会体现在 B 的代码参数和实现中，但它们的确定依据分别来自：
项目主要依据LLR 位宽A 的定点性能扫描内部位宽A 的溢出、量化和性能测试PE 并行度吞吐率、时延和 FPGA 资源要求存储架构PE 数量、存储端口、器件资源和时序N、冻结位置协议f/g 数学关系SC 算法和协议要求

3. 纯 B 可以单独写吗？
可以，而且应该先单独写。
纯 B 不能自己完成一个完整码块的 SC 译码，是因为它不知道：


当前应该执行 f 还是 g；


读取哪些地址；


结果写到哪里；


什么时候到达叶节点；


什么时候更新部分和；


下一步回到哪一层。


这些是 C 调度 FSM 的工作。
但 B 完全可以被写成一个由“微操作命令”驱动的数据通路。
可以理解为：
Plain textC：发命令B：执行命令并返回结果

建议的 B 模块划分
3.1 sc_pe
只负责一组 f/g：
verilogmodule sc_pe #(    parameter integer W = 10)(    input  wire signed [W-1:0] a,    input  wire signed [W-1:0] b,    input  wire                mode_g,    input  wire                beta,    output wire signed [W-1:0] y);
这个模块可以完全单独仿真。
测试内容包括：


f 同号；


f 异号；


正负数绝对值；


最小负数；


g=b+a；


g=b-a；


正负溢出；


饱和处理。



3.2 sc_llr_mem
负责串行装入信道 LLR，以及译码中间 LLR 的读写：
verilogmodule sc_llr_mem #(    parameter integer NMAX  = 1024,    parameter integer LLR_W = 10,    parameter integer AW    = 14)(    input  wire                         clk,    input  wire                         load_en,    input  wire [10:0]                  load_addr,    input  wire signed [LLR_W-1:0]      load_data,    input  wire                         rd_en,    input  wire [AW-1:0]                rd_addr_a,    input  wire [AW-1:0]                rd_addr_b,    output wire signed [LLR_W-1:0]      rd_data_a,    output wire signed [LLR_W-1:0]      rd_data_b,    input  wire                         wr_en,    input  wire [AW-1:0]                wr_addr,    input  wire signed [LLR_W-1:0]      wr_data);
地址如何生成由 C 决定，B 只按地址读写。

3.3 sc_partial_sum_mem
负责保存部分和：
verilogmodule sc_partial_sum_mem #(    parameter integer NMAX = 1024,    parameter integer AW   = 14)(    input  wire          clk,    input  wire          rd_en,    input  wire [AW-1:0] rd_addr,    output wire          rd_beta,    input  wire          wr_en,    input  wire [AW-1:0] wr_addr,    input  wire          wr_beta);
也可以一次读写两个部分和，具体取决于后续的部分和更新结构。

3.4 sc_datapath
把 PE、LLR 存储、部分和存储组合起来：
verilogmodule sc_datapath #(    parameter integer NMAX  = 1024,    parameter integer LLR_W = 8,    parameter integer INT_W = 10,    parameter integer PE_NUM = 1)(    input  wire clk,    input  wire rst_n,    // 串行根节点 LLR 装载    input  wire signed [LLR_W-1:0] llr_in,    input  wire                    llr_valid,    output wire                    llr_ready,    // C 发出的 PE 操作    input  wire                    op_valid,    input  wire                    op_mode_g,    input  wire [ADDR_W-1:0]       op_addr_a,    input  wire [ADDR_W-1:0]       op_addr_b,    input  wire [ADDR_W-1:0]       op_addr_y,    input  wire                    op_beta,    output wire                    op_ready,    output wire                    op_done,    // 叶节点接口    input  wire                    leaf_valid,    input  wire [10:0]             leaf_index,    input  wire                    leaf_frozen,    // C 控制部分和更新    input  wire                    beta_wr_en,    input  wire [ADDR_W-1:0]       beta_wr_addr,    input  wire                    beta_wr_data,    // 完成后串行输出 u_hat    input  wire                    out_start,    output wire                    u_valid,    output wire                    u_bit,    output wire [10:0]             u_index,    output wire                    u_last,    input  wire                    u_ready);
上面只是接口思想，最终还要根据选择的存储布局确定地址宽度和具体信号。

B 单独怎么仿真？
虽然 B 没有 C 不能自己遍历 SC 树，但测试平台可以暂时模拟 C。
例如 testbench 直接发送：
Plain text命令1：读取地址 a、b，执行 f，写地址 c命令2：读取地址 d、e，执行 g，beta=0，写地址 f命令3：写部分和命令4：读取叶节点 LLR
这样能验证：


LLR 是否装载到了正确地址；


PE 计算是否正确；


写回地址是否正确；


部分和存取是否正确；


串行输出顺序是否正确。


因此严格来说：

B 不能独立完成“SC 算法级仿真”，但可以独立完成“数据通路级仿真”。

等 C 写完以后，再做：
Plain textB + C 联合 SC 译码仿真
最后接上 D：
Plain textB + C + D 完整链路仿真

当前建议先锁定的初版方案
为了避免一开始设计过重，建议第一版按以下配置写：
Plain text输入接口：LLR 串行装载输出接口：u_hat 串行输出输出方式：全部译完后连续输出PE_NUM：1输入 LLR：参数化，暂定 8 位内部 LLR：参数化，暂定 10 位f 算法：min-sumg 算法：饱和加减LLR 存储：先用寄存器数组建模部分和：寄存器数组码长：64/128/256/512/1024位序：自然顺序，不做 bit reversal
等 A 给出定点扫描结果后，只需要调整：
verilogLLR_WINT_W饱和范围
而不需要重写整个数据通路。

---

### User

那先按照你推荐的第一版配置写，你现在还需要我给你提供哪部分代码，还是你已经能直接帮我写第一版了

---

### ChatGPT

按照我们刚才确定的第一版配置，我已经可以开始写 B 模块了，但是建议不要直接写完整 polar_sc_core，因为还缺几个会影响正确性的关键约束。
目前我已经确定的部分：
✅ 接口：


LLR 串行输入


译码结果串行输出


PE_NUM=1


min-sum f


g=b±a


参数化 LLR 位宽


寄存器型 LLR/β 存储


支持 N=64/128/256/512/1024


不做 bit reversal


这些可以直接写。

但是为了保证后面和你的编码器真正互通，还需要你补充下面几个东西（按重要程度排序）。

1. frozen_gen（最重要）
你前面顶层里面有：
verilogfrozen_gen #(.NMAX(NMAX)) u_fg (    .clk(clk),    .rst_n(rst_n),    .start(fg_start),    .n_log(nlog_blk),    .K(Kblk),    .busy(fg_busy),    .done(fg_done),    .frozen(fg_frozen));
SC 译码必须使用完全一样的冻结位置。
所以需要你提供：
verilogfrozen_gen.v
原因：
编码：
$u_i=0,\quad frozen_i=1$
译码：
verilogif(frozen[i])    u_hat[i]=0;else    根据LLR判决
如果冻结序列哪怕错一个位置：


无噪声环回都会失败；


SC 输出全部错乱。


这是第一优先级。

2. code_block_concat
你现在编码端输出：
verilogm_validm_bitk_count
译码端输入的 LLR 顺序必须对应：
Plain text发送顺序：d[0]d[1]...d[N-1]
但是我还不知道你的：
verilogcode_block_concat
有没有：


位倒序；


高位先发送；


多码块拼接顺序。


所以需要：
verilogcode_block_concat.v
否则 SC 输入顺序可能反了。

3. crc_gen（不是必须，但是后面需要）
B 模块本身不用 CRC。
但是后面 D 要做：
Plain textSC输出 ↓非冻结位抽取 ↓CRC24检查
所以之后需要：
verilogcrc_gen.v
现在可以晚一点给。

4. 你们 C 模块负责什么程度？
这个很关键。
你之前说：

我们只负责图片中 B 的部分

那么我需要知道你们交付边界。
有两种情况：

情况 A：B 只提供数据通路（推荐）
结构：
代码C模块 | | f/g命令 | ↓B模块 | ├── PE ├── LLR RAM ├── partial sum └── 输出u_hat
那么我会写：
代码sc_pe.vsc_llr_mem.vsc_beta_mem.vsc_datapath.v
接口留给 C 调。

情况 B：B 包含完整 SC
也就是：
代码polar_sc_core      |      ├── 自动遍历树      ├── 自动产生f/g      ├── 自动判决      └── 输出u_hat
那实际上 B+C 合并了。
开发会简单很多，但是和你图里的任务划分不一致。

5. 我建议现在先写哪几个文件？
按照工程实现顺序，我建议：
第一阶段（现在就可以写）
代码sc_pe.v
验证：


f 运算


g 运算


定点截断


↓
代码sc_llr_mem.v
验证：


LLR串行装载


地址读写


↓
代码sc_beta_mem.v
验证：


部分和保存



第二阶段
写：
代码sc_datapath.v
把三个东西连接起来。

第三阶段
等你给 C：
代码sc_controller.v
联合成：
代码polar_sc_decoder.v

所以现在我的建议：
你下一步发我：


frozen_gen.v ⭐⭐⭐⭐⭐


code_block_concat.v ⭐⭐⭐⭐


你们图中 B/C 的边界（如果有任务书截图最好）⭐⭐⭐⭐⭐


然后我就可以开始写第一版 B。
如果暂时不想发这些，我也可以先写纯数据通路版 sc_pe + LLR memory + beta memory，它不依赖你的工程代码，可以直接作为后续 SC 内核底层。

---

### User

1.module frozen_gen #(
    parameter integer NMAX = 1024
)(
    input  wire             clk,
    input  wire             rst_n,
    input  wire             start,
    input  wire [3:0]       n_log,     // log2(N); spec 取 6..10, 也支持更小便于测试
    input  wire [10:0]      K,         // 信息位数 (0..N)
    output reg              busy,
    output reg              done,
    output reg [NMAX-1:0]   frozen     // 1=冻结(u=0), 0=信息位; 位>=N 恒为1
);
    localparam integer RW = $clog2(NMAX);   // 秩位宽 (=10)
    localparam [RW-1:0] RANK_MAX = {RW{1'b1}};   // = NMAX-1 (NMAX 为 2 的幂)

    reg  [RW-1:0] rank;
    wire [9:0]    q;                        // 当前秩对应的比特索引
    reg  [11:0]   need;                     // 尚需标记的信息位数
    wire [11:0]   N = (12'd1 << n_log);     // 码长 N

    polar_reliability_rom u_rom (.rank(rank), .q(q));

    always @(posedge clk or negedge rst_n) begin
        if (!rst_n) begin
            busy   <= 1'b0;
            done   <= 1'b0;
            frozen <= {NMAX{1'b1}};
            rank   <= {RW{1'b0}};
            need   <= 12'd0;
        end else begin
            done <= 1'b0;
            if (start && !busy) begin
                frozen <= {NMAX{1'b1}};      // 全部先置冻结
                need   <= {1'b0, K};
                rank   <= RANK_MAX;          // 从最高可靠度秩开始
                busy   <= (K != 0);          // K=0 则无需处理
                done   <= (K == 0);          // K=0 立即完成 (全冻结)
            end else if (busy) begin
                if (({2'b00, q} < N) && (need != 0)) begin
                    frozen[q] <= 1'b0;        // 标为信息位
                    if (need == 12'd1) begin  // 刚标满 K 个 -> 完成
                        busy <= 1'b0;
                        done <= 1'b1;
                    end else begin
                        need <= need - 1'b1;
                        rank <= rank - 1'b1;
                    end
                end else begin
                    if (rank == 0) begin      // 扫到底 (K>N 的异常保护) -> 结束
                        busy <= 1'b0;
                        done <= 1'b1;
                    end else begin
                        rank <= rank - 1'b1;
                    end
                end
            end
        end
    end
endmodule
2.module code_block_concat #(
    parameter integer NMAX = 1024
)(
    input  wire             clk,
    input  wire             rst_n,
    // 装入一个码块
    input  wire             s_load,       // s_ready=1 时的装入脉冲
    input  wire [NMAX-1:0]  s_blk,        // 码块比特 f_r, bit j = f_r(j)
    input  wire [10:0]      s_elen,       // 该块有效比特数 E_r
    input  wire             s_last,       // 是否最后一块
    output reg              s_ready,       // 可接收下一块
    // 串行拼接输出
    output reg              m_valid,
    output reg              m_bit,
    output reg [31:0]       k_count,
    output reg              done          // 最后一块输出完毕 (单拍)
);
    reg [NMAX-1:0] blk;
    reg [10:0]     elen, j;
    reg            last_r;
    localparam IDLE=1'b0, EMIT=1'b1;
    reg st;

    always @(posedge clk or negedge rst_n) begin
        if (!rst_n) begin
            s_ready<=1'b1; m_valid<=1'b0; m_bit<=1'b0; k_count<=32'd0;
            done<=1'b0; j<=11'd0; st<=IDLE;
        end else begin
            done<=1'b0; m_valid<=1'b0;
            case (st)
                IDLE: begin
                    s_ready <= 1'b1;
                    if (s_load) begin
                        blk<=s_blk; elen<=s_elen; last_r<=s_last;
                        j<=11'd0; s_ready<=1'b0; st<=EMIT;
                    end
                end
                EMIT: begin
                    m_valid <= 1'b1;
                    m_bit   <= blk[j[9:0]];        // g[k] = f_r(j)  (j<=1023, 取低10位索引)
                    k_count <= k_count + 1'b1;
                    if (j == elen-1) begin
                        if (last_r) done <= 1'b1;
                        st <= IDLE;                // 本块输出完, 回 IDLE 等下一块
                    end else begin
                        j <= j + 1'b1;
                    end
                end
            endcase
        end
    end
endmodule
3. 现在我是负责写B的，是单独写B工作量小还是BC一起写小，或者说哪种你写起来更快

---

### ChatGPT

你现在给的两个模块已经确认了两个关键点：


frozen_gen 的冻结位索引是自然顺序：frozen[i] 对应 u[i]。


code_block_concat 按 d[0]、d[1]……d[N-1] 发送，所以 SC 端第 0 个 LLR 对应 d[0]，不需要额外比特反转。


因此，我现在已经可以直接写第一版，不再需要其他编码端代码。
B 单独写还是 B+C 一起写？
从代码量看：单独写 B 更小
纯 B 主要包含：
Plain textsc_peLLR 存储部分和存储判决位存储串行 LLR 装载串行 u_hat 输出数据通路控制接口
不需要实现：
Plain textSC 树遍历层级控制f/g 运算顺序存储地址生成部分和回传调度
这些属于 C。
所以严格按照任务分工，B 单独写的工作量更小。

从“尽快跑通完整译码”看：B+C 一起写更快
虽然 B 单独代码少，但纯 B 不能自主完成译码。写完后只能由 testbench 模拟 C，逐条发送类似命令：
Plain text读两个 LLR执行 f写回结果读两个 LLR执行 g写回结果进行叶节点判决更新部分和
如果 C 的接口后面发生变化，B 的地址和控制接口可能还要跟着改。
而 B+C 一起写时，可以直接做成：
Plain text串行装入 N 个 LLR        ↓自动执行完整 SC        ↓串行输出 N 个 u_hat
这样无噪声环回、带噪声测试都更容易，整体更快看到结果。
不过这样实际上会把你们图中 B 和 C 的任务合并，不适合直接作为“只负责 B”的最终交付。

最推荐的方式
建议采用中间方案：

正式代码只写 B，但额外写一个简化的仿真控制器，只用于验证，不作为 C 的正式交付。

结构如下：
Plain text正式交付：sc_pe.vsc_llr_mem.vsc_beta_mem.vsc_datapath.v仅用于仿真：sc_datapath_tb.vsc_test_sequencer.v
这样有三个好处：


不越过你负责的 B 范围；


B 可以独立完成数据通路级仿真；


C 后续只需按照已经验证过的控制接口接入。


这是当前最稳妥、返工风险最低的方案。
第一版 B 的配置
我会按以下配置实现：
Plain text码长：N=64/128/256/512/1024输入：串行 LLR输入顺序：d[0] → d[N-1]输出：串行 u_hat输出顺序：u_hat[0] → u_hat[N-1]PE 数量：1f 运算：min-sumg 运算：b ± a输入 LLR 位宽：8 位，参数化内部 LLR 位宽：10 位，参数化溢出方式：饱和运算LLR 存储：寄存器数组部分和存储：寄存器数组索引顺序：自然顺序，不做 bit reversal输出方式：全部判决完成后连续串行输出
B 顶层的职责边界
B 顶层建议叫：
verilogsc_datapath
它不自行决定下一步执行什么，而是接收 C 的微操作命令。
主要接口包括：
verilog// 串行根节点 LLR 装载llr_validllr_readyllr_in// f/g 运算命令op_validop_mode_gop_addr_aop_addr_bop_addr_yop_betaop_done// 叶节点判决命令leaf_validleaf_indexleaf_frozenleaf_done// 部分和读写命令beta_rd_addrbeta_rd_databeta_wr_enbeta_wr_addrbeta_wr_data// 串行输出out_startu_validu_readyu_bitu_indexu_last
其中 C 负责告诉 B：
Plain text从哪里读取 a、b本拍执行 f 还是 g结果写到哪里g 使用哪个 beta何时进行叶节点判决何时更新部分和
B 负责正确执行。
结论


只比较代码量：B 单独写更小。


只比较完整功能跑通速度：B+C 一起写更快。


考虑你的任务分工和后续联调：写纯 B + 仿真用简化控制器最好。


我现在不需要你再提供其他代码，可以直接按照这个方案开始写第一版 B。

---

### User

好了，现在帮写纯 B + 仿真用简化控制器的完整代码，你可以一个模块一个模块给我

---

### ChatGPT

先从最底层的 sc_pe.v 开始。它是 B 数据通路中最核心、也最容易独立验证的模块，完成：


mode_g=0：min-sum 的 f(a,b)；


mode_g=1：g(a,b,β)=b±a；


输入、输出均为 W 位有符号数；


对 f/g 结果进行有符号饱和；


对最小负数取绝对值时进行特殊处理，避免二进制补码溢出。


模块 1：sc_pe.v
verilog`timescale 1ns/1ps// ============================================================// SC 译码 Processing Element//// 功能：//   mode_g = 0://       f(a,b) ≈ sign(a)*sign(b)*min(|a|,|b|)//       使用 min-sum 近似////   mode_g = 1://       g(a,b,beta) = b + (1-2*beta)*a//                   = beta ? (b-a) : (b+a)//// LLR 符号约定：//   LLR >= 0：更倾向于比特 0//   LLR <  0：更倾向于比特 1//// 定点处理：//   输入输出均为 W 位有符号数。//   g 运算先扩展到 W+1 位，再饱和到 W 位。//   f 运算对最小负数 -2^(W-1) 的绝对值做饱和处理。// ============================================================module sc_pe #(    parameter integer W = 10)(    input  wire signed [W-1:0] a,    input  wire signed [W-1:0] b,    input  wire                mode_g,   // 0=f，1=g    input  wire                beta,     // g 运算所需部分和    output reg  signed [W-1:0] y);    // --------------------------------------------------------    // W 位有符号数范围    //    // MAX_POS =  2^(W-1)-1    // MIN_NEG = -2^(W-1)    // --------------------------------------------------------    localparam signed [W-1:0] MAX_POS = {        1'b0, {(W-1){1'b1}}    };    localparam signed [W-1:0] MIN_NEG = {        1'b1, {(W-1){1'b0}}    };    // 扩展一位后的饱和边界    localparam signed [W:0] MAX_EXT = {        2'b00, {(W-1){1'b1}}    };    localparam signed [W:0] MIN_EXT = {        2'b11, {(W-1){1'b0}}    };    // --------------------------------------------------------    // f 运算相关信号    //    // 绝对值使用 W 位无符号数表示。    // 对 MIN_NEG，其数学绝对值为 2^(W-1)，W 位无符号数可表示。    // --------------------------------------------------------    reg [W-1:0] abs_a;    reg [W-1:0] abs_b;    reg [W-1:0] min_abs;    reg signed [W-1:0] f_result;    // --------------------------------------------------------    // g 运算使用 W+1 位，防止中间加减立即溢出    // --------------------------------------------------------    reg signed [W:0] a_ext;    reg signed [W:0] b_ext;    reg signed [W:0] g_ext;    reg signed [W-1:0] g_result;    always @* begin        // 默认值，避免组合逻辑锁存        abs_a    = {W{1'b0}};        abs_b    = {W{1'b0}};        min_abs  = {W{1'b0}};        f_result = {W{1'b0}};        a_ext    = {W+1{1'b0}};        b_ext    = {W+1{1'b0}};        g_ext    = {W+1{1'b0}};        g_result = {W{1'b0}};        // ====================================================        // f：min-sum        // ====================================================        // 计算 |a|        //        // 使用无符号补码求绝对值：        //   负数：~a + 1        //   非负：a        //        // 对 MIN_NEG：        //   例如 W=10，a=-512，结果 abs_a=512，        //   可以由 10 位无符号数表示。        if (a[W-1])            abs_a = (~a) + {{(W-1){1'b0}}, 1'b1};        else            abs_a = a;        // 计算 |b|        if (b[W-1])            abs_b = (~b) + {{(W-1){1'b0}}, 1'b1};        else            abs_b = b;        // 取较小幅度        if (abs_a <= abs_b)            min_abs = abs_a;        else            min_abs = abs_b;        // f 输出符号 = a、b 符号异或        if (a[W-1] ^ b[W-1]) begin            // 负结果            //            // 当 min_abs = 2^(W-1) 时，结果恰好为 MIN_NEG，            // 可以直接通过二进制补码表示。            f_result = (~min_abs) + {{(W-1){1'b0}}, 1'b1};        end else begin            // 正结果            //            // 当 min_abs = 2^(W-1) 时，正数无法用 W 位有符号数表示，            // 因此饱和到 MAX_POS。            if (min_abs[W-1])                f_result = MAX_POS;            else                f_result = $signed(min_abs);        end        // ====================================================        // g：b ± a        // ====================================================        a_ext = {a[W-1], a};        b_ext = {b[W-1], b};        if (beta)            g_ext = b_ext - a_ext;        else            g_ext = b_ext + a_ext;        // 正溢出饱和        if (g_ext > MAX_EXT)            g_result = MAX_POS;        // 负溢出饱和        else if (g_ext < MIN_EXT)            g_result = MIN_NEG;        // 未溢出，保留低 W 位        else            g_result = g_ext[W-1:0];        // ====================================================        // 输出选择        // ====================================================        if (mode_g)            y = g_result;        else            y = f_result;    endendmodule
关键行为举例
假设 W=10，范围为：
Plain text-512 ～ +511
f 运算
Plain texta =  100，b =  40  → f =  40a = -100，b =  40  → f = -40a = -100，b = -40  → f =  40
极限情况：
Plain texta = -512，b = -512
理论结果符号为正、幅值为 512，但 +512 无法用 10 位有符号数表示，因此输出：
Plain text+511
g 运算
Plain texta=100，b=40，beta=0 → 40+100 = 140a=100，b=40，beta=1 → 40-100 = -60
饱和情况：
Plain texta=400，b=300，beta=0理论结果=700输出=511
下一模块应写 sc_pe_tb.v，先把 f、g、边界值和饱和行为完整验证，再进入 LLR 存储模块。

---

### User

你总结当前对话，我想开启一个新对话，帮我生成一个命令，包含你新对话帮我写代码需要的信息等，可以让我完美过渡到新对话

---

### ChatGPT

把下面整段复制到新对话即可。它包含项目背景、已确认的位序、B/C边界、第一版架构和后续代码顺序。
写入我正在为一套 Polar 编码程序设计 SC 译码器。请直接承接以下上下文继续工作，不要重复询问已经明确的信息。一、任务范围项目划分图中，我们只负责 B 部分：SC 译码数据通路，主要包括：PE，即执行 f/g 运算的 Processing Element；LLR 存储；部分和 β 存储及相关数据通路；判决位 u_hat 存储；串行 LLR 装载；译码结果串行输出；面向 C 控制模块的微操作接口。C 部分负责：SC 树深度优先遍历；f/g 运算顺序控制；LLR 存储读写地址生成；叶节点到达时机；部分和回传和更新时序。当前要求是：编写纯 B 数据通路，同时额外编写一个仅用于仿真的简化控制器或 test sequencer，使 B 可以单独完成数据通路级验证。简化控制器不是正式 C 模块交付。请采用 Verilog-2001，不要使用必须依赖 SystemVerilog 的二维端口语法。二、已经确定的第一版配置请严格按以下配置开发：最大码长：NMAX=1024支持码长：64、128、256、512、1024n_log 取值：6～10输入方式：串行输入 LLR输入顺序：第 0 个 LLR 对应码字 d[0]，随后为 d[1]...d[N-1]输出方式：译码全部完成后，连续串行输出 u_hat[0]...u_hat[N-1]输出包含冻结位；冻结位应输出 0输出建议采用 valid/readyPE 数量：第一版固定 PE_NUM=1输入 LLR 位宽：参数化，默认 LLR_W=8内部 LLR 位宽：参数化，默认 INT_W=10LLR 符号约定：LLR 大于或等于 0，更倾向比特 0LLR 小于 0，更倾向比特 1f 运算：min-sum 近似g 运算：g(a,b,beta)=beta ? b-a : b+af/g 运算需要有符号饱和LLR 存储：第一版使用寄存器数组建模部分和存储：寄存器数组判决位存储：寄存器数组不执行 bit reversal索引使用自然顺序后续可替换同步 RAM/BRAM，但第一版先保证功能正确三、编码端位序已经确认编码器蝶形网络如下：for (gs = 0; gs < SMAX; gs = gs + 1) begin
    localparam integer M = (1 << gs);
    for (gi = 0; gi < NMAX; gi = gi + 2*M) begin
        for (gj = 0; gj < M; gj = gj + 1) begin
            assign stage_net[gs][gi+gj]
                = x[gi+gj] ^ x[gi+gj+M];

            assign stage_net[gs][gi+gj+M]
                = x[gi+gj+M];
        end
    end
end编码器实现的是：d = u · F^(⊗n)没有显式执行 B_N 比特反转。例如 N=4 时：d[0] = u0 ^ u1 ^ u2 ^ u3
d[1] = u1 ^ u3
d[2] = u2 ^ u3
d[3] = u3码块串行输出模块为：m_bit <= blk[j[9:0]];其中 j 从 0 递增，因此串行发送顺序明确为：d[0]、d[1]、……、d[N-1]SC 输入 LLR 必须按同样顺序存入根节点，不能反序，也不能额外做 bit reversal。四、冻结集约定已有 frozen_gen 的输出定义：output reg [NMAX-1:0] frozen;其中：frozen[i] = 1：u[i] 为冻结位，强制判决为 0
frozen[i] = 0：u[i] 为信息位，根据叶节点 LLR 判决frozen[i] 与 u[i] 使用相同的自然索引。叶节点判决规则：if (frozen[phi])
    u_hat[phi] <= 1'b0;
else
    u_hat[phi] <= leaf_llr[INT_W-1];因为内部约定负 LLR 对应比特 1。冻结集的生成不属于 B。B 可以接收由 C 或外部提供的当前叶节点 leaf_frozen，也可以在装载阶段缓存整个 frozen，但不要在 B 中重新实现可靠度序列。五、CRC和码块处理不属于B类型2在当前 Polar 编码链内部没有为每个 Polar 码块附加 CRC24B，但上层输入消息可能已经包含帧级或传输块级 CRC。无论如何，以下内容不属于 B：码块划分；CRC 生成或校验；非冻结信息位抽取；去前置补零；多码块消息拼接。B 只负责输出全部 u_hat[0:N-1]。六、PE定义PE 是 Processing Element，执行两种运算。f运算使用 min-sum：f(a,b) = sign(a) × sign(b) × min(|a|,|b|)g运算g(a,b,beta) = b + (1-2beta)a等价于：g = beta ? (b-a) : (b+a);这里的 beta 是已经译出的左子树部分和，不是当前叶节点判决位。七、已经完成的第一个模块之前已经给出 sc_pe.v 的设计，功能要求如下：module sc_pe #(
    parameter integer W = 10
)(
    input  wire signed [W-1:0] a,
    input  wire signed [W-1:0] b,
    input  wire                mode_g,
    input  wire                beta,
    output reg  signed [W-1:0] y
);要求：mode_g=0 执行 min-sum f；mode_g=1 执行 g；输入输出均为 W 位有符号数；g 使用 W+1 位中间结果后饱和；f 正向结果超过最大正数时饱和；正确处理最小负数，例如 W=10 时的 -512；W=10 时输出范围为 -512～511。不要重新设计完全不同的 PE 接口，除非发现明确错误。发现错误时应指出并给出修正版完整代码。八、接下来需要依次编写的完整代码请一个模块一个模块给出，每次给出：完整、可综合代码；清晰中文注释；接口说明；关键时序说明；对应的独立 testbench；仿真中应看到的预期结果；发现前序架构问题时及时说明。建议顺序为：第1步sc_pe_tb.v全面验证：f 同号；f 异号；f 输入为 0；f 最小负数；g 加法；g 减法；g 正溢出；g 负溢出；随机测试；最好在 testbench 中用整数参考函数自动比较，而不是只打印波形。第2步sc_llr_mem.v
sc_llr_mem_tb.v功能包括：串行根节点 LLR 装载；已知 N=1<<n_log；第一个有效输入写入根节点 d[0] 对应位置；支持中间 LLR 的地址读写；第一版采用寄存器数组；明确根节点和中间层 LLR 的地址布局；地址布局必须方便后续 C 产生地址；PE_NUM=1，但一次 f/g 需要两个源操作数，所以需要合理的双读方式；可以采用组合双读、时钟写入；后续应能替换为同步 RAM。第3步sc_beta_mem.v
sc_beta_mem_tb.v功能包括：存储 SC 部分和；支持 C 指定地址读写；明确部分和地址布局；支持部分和更新过程中需要的两个子节点值；第一版使用寄存器数组；不要把完整树遍历控制写入该模块。第4步sc_uhat_mem.v
sc_uhat_mem_tb.v功能包括：保存叶节点判决；支持按索引写入 u_hat[phi]；完成后连续串行输出；输出接口建议：output wire        u_valid;
input  wire        u_ready;
output wire        u_bit;
output wire [10:0] u_index;
output wire        u_last;输出顺序固定为 u_hat[0] 到 u_hat[N-1]。第5步sc_datapath.v
sc_datapath_tb.v把以下模块组合：sc_pesc_llr_memsc_beta_memsc_uhat_memsc_datapath 只接收 C 发出的微操作，不自行遍历 SC 树。请为 C 设计清晰的命令接口，至少支持：串行装入根节点 LLR；发起一次 f/g 运算；指定两个 LLR 源地址；指定结果写回地址；指定 mode_g；指定 g 所需 beta，或者从 beta memory 指定地址读取；发起一次叶节点判决；指定叶节点索引；指定该叶节点是否冻结；将判决结果写入 u_hat；读写部分和；启动串行输出。接口需要有明确的 valid/ready/done 或单拍命令规范，避免重复执行命令。第6步编写仅用于仿真的简化控制器：sc_test_sequencer.v
sc_b_only_system_tb.v该简化控制器可以：模拟 C 向 B 发出若干 f/g、判决、部分和读写命令；验证完整的数据通路动作；不要求一开始实现正式高性能 C；至少完成 N=2 或 N=4 的完整 SC 数据通路演示；最好能够完成一个无噪声小码长 Polar 码的端到端验证；但正式交付仍应明确 sc_test_sequencer 只是仿真辅助代码。九、无噪声环回的含义无噪声环回只用于第一阶段逻辑验证：消息 → Polar编码 → 码字比特 → 理想LLR → SC译码硬比特转理想 LLR 示例：llr = encoded_bit ? -8'sd32 : 8'sd32;无噪声环回用于检查：位序；frozen对应关系；f/g顺序；部分和；u_hat索引。后续正式性能仿真会使用 BPSK+AWGN：0 → +1
1 → -1接收值为 y 时：LLR = 2y / sigma²但 AWGN 信道仿真不属于纯 B RTL 的主要交付，可在系统 testbench 或软件参考模型中完成。十、代码风格要求使用 Verilog-2001；兼容 Icarus Verilog、Vivado 或常见 RTL 仿真器；不使用不可综合的 RTL 结构，testbench 除外；每个时序 always 块使用非阻塞赋值；组合逻辑避免锁存；对有符号比较、扩展和切片进行明确处理；所有参数和地址宽度需要合法；对 N=64～1024 均不能发生计数器截断；必须解释存储地址布局；不要只给伪代码；不要省略模块；不要一次性丢出所有模块，请严格一个模块一个模块推进；当前第一步请直接给出完整的 sc_pe_tb.v，并检查之前 sc_pe.v 的饱和边界和有符号比较是否存在问题。
这份提示词会让新对话从 sc_pe_tb.v 和 PE代码复核处无缝继续。

我正在为一套 Polar 编码程序设计 SC 译码器。请直接承接以下上下文继续工作，不要重复询问已经明确的信息。一、任务范围项目划分图中，我们只负责 B 部分：SC 译码数据通路，主要包括：PE，即执行 f/g 运算的 Processing Element；LLR 存储；部分和 β 存储及相关数据通路；判决位 u_hat 存储；串行 LLR 装载；译码结果串行输出；面向 C 控制模块的微操作接口。C 部分负责：SC 树深度优先遍历；f/g 运算顺序控制；LLR 存储读写地址生成；叶节点到达时机；部分和回传和更新时序。当前要求是：编写纯 B 数据通路，同时额外编写一个仅用于仿真的简化控制器或 test sequencer，使 B 可以单独完成数据通路级验证。简化控制器不是正式 C 模块交付。请采用 Verilog-2001，不要使用必须依赖 SystemVerilog 的二维端口语法。二、已经确定的第一版配置请严格按以下配置开发：最大码长：NMAX=1024支持码长：64、128、256、512、1024n_log 取值：6～10输入方式：串行输入 LLR输入顺序：第 0 个 LLR 对应码字 d[0]，随后为 d[1]...d[N-1]输出方式：译码全部完成后，连续串行输出 u_hat[0]...u_hat[N-1]输出包含冻结位；冻结位应输出 0输出建议采用 valid/readyPE 数量：第一版固定 PE_NUM=1输入 LLR 位宽：参数化，默认 LLR_W=8内部 LLR 位宽：参数化，默认 INT_W=10LLR 符号约定：LLR 大于或等于 0，更倾向比特 0LLR 小于 0，更倾向比特 1f 运算：min-sum 近似g 运算：g(a,b,beta)=beta ? b-a : b+af/g 运算需要有符号饱和LLR 存储：第一版使用寄存器数组建模部分和存储：寄存器数组判决位存储：寄存器数组不执行 bit reversal索引使用自然顺序后续可替换同步 RAM/BRAM，但第一版先保证功能正确三、编码端位序已经确认编码器蝶形网络如下：for (gs = 0; gs < SMAX; gs = gs + 1) begin
    localparam integer M = (1 << gs);
    for (gi = 0; gi < NMAX; gi = gi + 2*M) begin
        for (gj = 0; gj < M; gj = gj + 1) begin
            assign stage_net[gs][gi+gj]
                = x[gi+gj] ^ x[gi+gj+M];

            assign stage_net[gs][gi+gj+M]
                = x[gi+gj+M];
        end
    end
end编码器实现的是：d = u · F^(⊗n)没有显式执行 B_N 比特反转。例如 N=4 时：d[0] = u0 ^ u1 ^ u2 ^ u3
d[1] = u1 ^ u3
d[2] = u2 ^ u3
d[3] = u3码块串行输出模块为：m_bit <= blk[j[9:0]];其中 j 从 0 递增，因此串行发送顺序明确为：d[0]、d[1]、……、d[N-1]SC 输入 LLR 必须按同样顺序存入根节点，不能反序，也不能额外做 bit reversal。四、冻结集约定已有 frozen_gen 的输出定义：output reg [NMAX-1:0] frozen;其中：frozen[i] = 1：u[i] 为冻结位，强制判决为 0
frozen[i] = 0：u[i] 为信息位，根据叶节点 LLR 判决frozen[i] 与 u[i] 使用相同的自然索引。叶节点判决规则：if (frozen[phi])
    u_hat[phi] <= 1'b0;
else
    u_hat[phi] <= leaf_llr[INT_W-1];因为内部约定负 LLR 对应比特 1。冻结集的生成不属于 B。B 可以接收由 C 或外部提供的当前叶节点 leaf_frozen，也可以在装载阶段缓存整个 frozen，但不要在 B 中重新实现可靠度序列。五、CRC和码块处理不属于B类型2在当前 Polar 编码链内部没有为每个 Polar 码块附加 CRC24B，但上层输入消息可能已经包含帧级或传输块级 CRC。无论如何，以下内容不属于 B：码块划分；CRC 生成或校验；非冻结信息位抽取；去前置补零；多码块消息拼接。B 只负责输出全部 u_hat[0:N-1]。六、PE定义PE 是 Processing Element，执行两种运算。f运算使用 min-sum：f(a,b) = sign(a) × sign(b) × min(|a|,|b|)g运算g(a,b,beta) = b + (1-2beta)a等价于：g = beta ? (b-a) : (b+a);这里的 beta 是已经译出的左子树部分和，不是当前叶节点判决位。七、已经完成的第一个模块之前已经给出 sc_pe.v 的设计，功能要求如下：module sc_pe #(
    parameter integer W = 10
)(
    input  wire signed [W-1:0] a,
    input  wire signed [W-1:0] b,
    input  wire                mode_g,
    input  wire                beta,
    output reg  signed [W-1:0] y
);要求：mode_g=0 执行 min-sum f；mode_g=1 执行 g；输入输出均为 W 位有符号数；g 使用 W+1 位中间结果后饱和；f 正向结果超过最大正数时饱和；正确处理最小负数，例如 W=10 时的 -512；W=10 时输出范围为 -512～511。不要重新设计完全不同的 PE 接口，除非发现明确错误。发现错误时应指出并给出修正版完整代码。八、接下来需要依次编写的完整代码请一个模块一个模块给出，每次给出：完整、可综合代码；清晰中文注释；接口说明；关键时序说明；对应的独立 testbench；仿真中应看到的预期结果；发现前序架构问题时及时说明。建议顺序为：第1步sc_pe_tb.v全面验证：f 同号；f 异号；f 输入为 0；f 最小负数；g 加法；g 减法；g 正溢出；g 负溢出；随机测试；最好在 testbench 中用整数参考函数自动比较，而不是只打印波形。第2步sc_llr_mem.v
sc_llr_mem_tb.v功能包括：串行根节点 LLR 装载；已知 N=1<<n_log；第一个有效输入写入根节点 d[0] 对应位置；支持中间 LLR 的地址读写；第一版采用寄存器数组；明确根节点和中间层 LLR 的地址布局；地址布局必须方便后续 C 产生地址；PE_NUM=1，但一次 f/g 需要两个源操作数，所以需要合理的双读方式；可以采用组合双读、时钟写入；后续应能替换为同步 RAM。第3步sc_beta_mem.v
sc_beta_mem_tb.v功能包括：存储 SC 部分和；支持 C 指定地址读写；明确部分和地址布局；支持部分和更新过程中需要的两个子节点值；第一版使用寄存器数组；不要把完整树遍历控制写入该模块。第4步sc_uhat_mem.v
sc_uhat_mem_tb.v功能包括：保存叶节点判决；支持按索引写入 u_hat[phi]；完成后连续串行输出；输出接口建议：output wire        u_valid;
input  wire        u_ready;
output wire        u_bit;
output wire [10:0] u_index;
output wire        u_last;输出顺序固定为 u_hat[0] 到 u_hat[N-1]。第5步sc_datapath.v
sc_datapath_tb.v把以下模块组合：sc_pesc_llr_memsc_beta_memsc_uhat_memsc_datapath 只接收 C 发出的微操作，不自行遍历 SC 树。请为 C 设计清晰的命令接口，至少支持：串行装入根节点 LLR；发起一次 f/g 运算；指定两个 LLR 源地址；指定结果写回地址；指定 mode_g；指定 g 所需 beta，或者从 beta memory 指定地址读取；发起一次叶节点判决；指定叶节点索引；指定该叶节点是否冻结；将判决结果写入 u_hat；读写部分和；启动串行输出。接口需要有明确的 valid/ready/done 或单拍命令规范，避免重复执行命令。第6步编写仅用于仿真的简化控制器：sc_test_sequencer.v
sc_b_only_system_tb.v该简化控制器可以：模拟 C 向 B 发出若干 f/g、判决、部分和读写命令；验证完整的数据通路动作；不要求一开始实现正式高性能 C；至少完成 N=2 或 N=4 的完整 SC 数据通路演示；最好能够完成一个无噪声小码长 Polar 码的端到端验证；但正式交付仍应明确 sc_test_sequencer 只是仿真辅助代码。九、无噪声环回的含义无噪声环回只用于第一阶段逻辑验证：消息 → Polar编码 → 码字比特 → 理想LLR → SC译码硬比特转理想 LLR 示例：llr = encoded_bit ? -8'sd32 : 8'sd32;无噪声环回用于检查：位序；frozen对应关系；f/g顺序；部分和；u_hat索引。后续正式性能仿真会使用 BPSK+AWGN：0 → +1
1 → -1接收值为 y 时：LLR = 2y / sigma²但 AWGN 信道仿真不属于纯 B RTL 的主要交付，可在系统 testbench 或软件参考模型中完成。十、代码风格要求使用 Verilog-2001；兼容 Icarus Verilog、Vivado 或常见 RTL 仿真器；不使用不可综合的 RTL 结构，testbench 除外；每个时序 always 块使用非阻塞赋值；组合逻辑避免锁存；对有符号比较、扩展和切片进行明确处理；所有参数和地址宽度需要合法；对 N=64～1024 均不能发生计数器截断；必须解释存储地址布局；不要只给伪代码；不要省略模块；不要一次性丢出所有模块，请严格一个模块一个模块推进；当前第一步请直接给出完整的 sc_pe_tb.v，并检查之前 sc_pe.v 的饱和边界和有符号比较是否存在问题。

